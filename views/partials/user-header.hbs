<header>
  <nav class="navbar navbar-expand-lg" style="background-color: #4784ff">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><img style="width: 100px;" src="/logo/logo3.png" alt="SHOPIFY"></a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active text-white" aria-current="page" href="/">Products</a>
          </li>
          {{#if user}}
          <li class="nav-item">
            <a class="nav-link active text-white" href="/my-orders">Orders</a>
          </li>
          {{/if}}
          <li class="nav-item">
            <a class="nav-link active text-white" href="/cart">Cart<span class="badge bg-danger" id="cart-count">{{cartCount}}</span></a>
          </li>

          <li class="nav-item">
            <a class="nav-link active text-white">Help</a>
          </li>
        </ul>
<div class="btn-group dropstart">
        <button type="button" class="btn dropdown-toggle text-white" data-bs-toggle="dropdown">
          {{#if user}}
          {{user.Name}}
          {{else}}
          My Account
          {{/if}}
        </button>
        <div class="dropdown-menu">
            
            <a href="/signup" class="dropdown-item">Creat New Account</a>
            
            {{#if user}}
            <a href="#" class="dropdown-item">My Profile</a>
            <a href="#" class="dropdown-item">Orders</a>
            <a href="#" class="dropdown-item">Address book</a>
            <a href="#" class="dropdown-item">Whishlist</a>
            <a href="#" class="dropdown-item">Coupons</a>
            <a href="#" class="dropdown-item">Notifications</a>
          <a href="/logout" class="btn btn-danger ms-2">Logout</a>
          {{else}}
          <a href="/login" class="dropdown-item">Login</a>
          {{/if}}
            
        </div>
    </div>

        {{!-- <form class="d-flex" role="search">
          <input
            class="form-control me-2"
            type="search"
            placeholder="Search"
            aria-label="Search"
          />
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form> --}}
      </div>
    </div>
  </nav>
</header>